# ── Phase 5 — Agentic AI Layer Dependencies ────────────────────────────────
# Install with: pip install -r requirements_phase5.txt

# LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0         # Groq LLM integration
langchain-community>=0.3.0       # Community integrations

# Vector DB + Embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0     # all-MiniLM-L6-v2 for local embeddings

# FastAPI + server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # for file upload support
websockets>=12.0

# Data (already installed from Phase 1-4, pinned for safety)
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0

# Pydantic (FastAPI dependency)
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0                    # async HTTP client (used internally by langchain)